name: test1

on:
  workflow_dispatch

jobs:
  s3-release-prod:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@d632683dd7b4114ad314bca15554477dd762a938 # v4.2.0
      with:
        submodules: true
        fetch-depth: 0
    - name: log
      run: |
        pwd; ls -la; echo "$PATH";
